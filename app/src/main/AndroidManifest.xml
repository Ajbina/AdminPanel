<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.adminpanel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Edit_NewsfeedActivity" />
        <activity android:name=".Edit_NoticeActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ADDPageAdapter" />
        <activity android:name=".ADDEventFragment" />
        <activity android:name=".AddInfo"></activity>
    </application>

</manifest>